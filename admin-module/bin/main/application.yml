spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/onde_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&createDatabaseIfNotExist=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: maria
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081

jwt:
  secret: your-admin-secret-key-here-must-be-very-long-and-secure-with-more-than-256-bits-length-spec
  access-token-expiration: 1800000
  refresh-token-expiration: 1209600000
