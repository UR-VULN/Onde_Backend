spring:
  datasource:
    url: jdbc:mysql://localhost:3306/onde_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&createDatabaseIfNotExist=true
    username: root
    password: maria
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081

jwt:
  secret: your-admin-secret-key-here
  access-token-expiration: 1800000
  refresh-token-expiration: 1209600000
